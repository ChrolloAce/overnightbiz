import { NextResponse } from 'next/server';

export async function GET() {
  return NextResponse.json({ status: 'Authentication API placeholder' });
}

export async function POST() {
  return NextResponse.json({ status: 'Authentication API placeholder' });
} 